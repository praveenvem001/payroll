apiVersion: v1
kind: Service
metadata:
    name: "microservicesakscluster-59af"
    labels:
        app: "microservicesakscluster-59af"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "microservicesakscluster-59af"